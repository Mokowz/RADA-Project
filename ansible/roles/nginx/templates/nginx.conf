server {
    listen 80;
    server_name {{ server_ip }};

    location /static/ {
        root {{ static_path }};
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}